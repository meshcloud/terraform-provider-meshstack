# See GitHub's documentation for more information on this file:
# https://docs.github.com/en/code-security/supply-chain-security/keeping-your-dependencies-updated-automatically/configuration-options-for-dependency-updates
version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"
    # Combine Go module updates into a single PR instead of one PR per module.
    groups:
      go-modules:
        patterns:
          - "*"

  # Only infra/github, which is real Terraform we deploy. The .tf files under examples/ document
  # this provider's own usage, and a bump there would rewrite the very versions the docs show.
  - package-ecosystem: "terraform"
    directory: "/infra/github"
    schedule:
      interval: "weekly"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Combine all GitHub Actions bumps into a single PR (including major versions),
    # so they can be reviewed and merged together rather than one PR per action.
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
          - "major"
